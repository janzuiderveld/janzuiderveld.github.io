export interface AsciiArtConfig {
  // Add any configuration options needed for the ASCII art renderer
  // This can be expanded based on specific needs
  maxWidth?: number;
  maxHeight?: number;
  aspectRatio?: number;
  characterSet?: string[];
  backgroundColor?: string;
  textColor?: string;
} 